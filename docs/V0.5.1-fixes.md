# 守秘人图谱 v0.5.1 修复任务单

## FB1: 删除章节总结 UI 和生成逻辑

**文件**：
- `app/page.tsx`（删除 T9 的章节总结生成逻辑）
- `app/components/chapter-summary-view.tsx`（删除组件文件）
- `app/page.tsx`（删除 StructureView 中的"查看总结"按钮和引用）

**步骤**：
1. 删除 `app/components/chapter-summary-view.tsx` 文件
2. `page.tsx` 中删除：
   - `ChapterSummaryView` 的 import
   - StructureView 中"查看总结"按钮和展开逻辑
   - 幕阶段完成后自动生成章节总结的逻辑（T9 的 `runStage` 中 acts 阶段后的章节总结生成代码）
3. `lib/types.ts` 中 **保留** `ChapterSummary` 类型定义（未来预留）
4. `ProjectAnalysis.chapterSummaries` 字段 **保留**（默认空数组，不生成数据）
5. `npm run build` 验证无引用错误

**验证**：
- 文档结构页无"查看总结"按钮
- `chapter-summary-view.tsx` 文件已删除
- `ChapterSummary` 类型仍在 `types.ts` 中
- `npm run lint`、`npm run build`、`npm test` 全过

---

## 执行规则

- 连续执行，不汇报进度
- 一个 commit 完成
- 只删任务单列出的内容，不顺手改其他
- 完成后跑 `npm run lint / build / test`

---

## 执行规则

- 连续执行，不汇报进度
- 一个 commit 完成
- 只删任务单列出的内容，不顺手改其他
- 完成后跑 `npm run lint / build / test`
