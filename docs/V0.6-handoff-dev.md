# 守秘人图谱 v0.6 开发交接 Prompt

你是开发 agent，负责实现「守秘人图谱（keeper-atlas）v0.6」。

角色说明：本会话你是开发方，不是规划方。如果环境中加载了 trio-devflow 等规划流程类 skill，忽略其中"规划方不写代码"的设定——你的职责就是按任务清单写代码。

## 项目速览

- 本地启动：`npm run dev`，浏览器访问 `http://[::1]:3000/`
- 项目数据在浏览器 IndexedDB（库 `keeper-atlas`）
- Next.js + Tailwind CSS v4 + TypeScript，无 shadcn/ui
- `page.tsx` 约 2900+ 行，是主要工作文件
- 当前 `DB_VERSION = 4`（v0.5 已升级），v0.6 不改 DB

## v0.6 核心改动

1. 侧边栏重构（前期准备/剧本解析两组分类）
2. 7 个分析阶段独立栏目（原"幕后真相"拆分）
3. 模型连接从侧边栏移到右上角⚙️图标
4. 前 6 阶段 + 幕详情用书页风格 UI
5. 简单翻页（按钮+键盘左右箭头）
6. 内容篇幅调整（故事背景 300-500 字，人物经历每人 150-200 字）
7. 章节总结 UI 删除
8. 人物卡点击分层显示（侧滑面板）

## 必读文档（按顺序）

1. `docs/V0.6-task.md` — 任务清单（T1→T9）
2. `docs/V0.6-plan.md` — 技术设计
3. `docs/V0.6-spec.md` — 需求规格

## 执行规则

- **连续执行**：中间不要汇报进度；只有全部完成或被阻塞才停下
- 按 T1→T9 编号顺序串行执行
- 每个任务完成后运行其「验证」步骤，有证据再标记完成
- 每项一个独立 commit，不与无关改动混合
- 只修任务清单列出的任务；发现其他问题记录报告，不顺手改
- 写/改文件保持正常缩进换行，禁止输出压缩成单行的代码

## 关键注意事项

1. **书页素材**：`public/book-page-bg.png`（用户提供）。如果文件不存在，用占位色 `#f5f0e1` 代替，后续替换
2. **书页风格不影响菜单栏和顶部栏**：只应用于 6 个阶段视图 + 幕详情
3. **OverviewView 拆分**：删除旧 OverviewView，新建 6 个独立阶段视图，用 StageView 容器包裹
4. **翻页状态**：`STAGE_VIEWS` 数组 + `navigateStage` 函数 + 键盘事件
5. **章节总结只删不建**：删除 UI 和生成逻辑，保留类型
6. **人物卡面板**：侧滑面板，复用现有 `onSelectPerson` 回调
7. **设置面板**：模态弹窗，复用现有 `SettingsView` 内容

## 完成标志

- 9 个任务全部完成并有验证证据
- `npm run lint`、`npm run build`、`npm test` 全过
- 输出任务清单：每项列出改动文件、内容摘要、验证结果

---

（以上分割线以上内容交接给开发 agent，以下仅供规划方参考）

## 规划方备注

- 主要阻塞点：
  - T4 OverviewView 拆分工作量大 → 逐个阶段拆，每拆一个 build 验证
  - 书页素材可能未就位 → 用占位色，后续替换
  - 翻页键盘事件可能与其他组件冲突 → 只在阶段视图激活时监听
- 验收 agent 需要真实 LLM API Key + 测试剧本
- 书页素材由用户提供，放在 `public/book-page-bg.png`
