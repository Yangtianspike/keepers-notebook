# 守秘人图谱 v0.6 任务拆解（Task）

对应：`docs/V0.6-spec.md`、`docs/V0.6-plan.md`（均已批准）。

---

## 依赖总览

```
T1 ──→ T2 ──→ T3 ──→ T4 ──→ T5
T6（独立）
T7 ──→ T8
T9（收尾，依赖全部）
```

---

## T1: 侧边栏重构 + View 类型扩展

**文件**：`app/page.tsx`
**依赖**：无

**步骤**：
1. `View` 类型扩展：新增 `"stage-background"` / `"stage-timeplace"` / `"stage-characters"` / `"stage-characterArcs"` / `"stage-openingHook"` / `"stage-clues"`；删除 `"overview"` 和 `"review"`
2. `navigation` 改为 `navGroups` 分组结构（前期准备/剧本解析两组）
3. 侧边栏渲染改为遍历 `navGroups`，每组有标题（`nav-label`）
4. 删除侧边栏"系统/模型连接"按钮

**验证**：
- 侧边栏显示两组分类
- 7 个阶段独立栏目可见
- 无"模型连接"侧边栏入口

---

## T2: 右上角设置图标 + 模态弹窗

**文件**：`app/components/settings-modal.tsx`（新建）、`app/page.tsx`
**依赖**：T1

**步骤**：
1. 新建 `settings-modal.tsx`：模态弹窗（半透明遮罩 + 居中面板），内容复用现有 `SettingsView`
2. `page.tsx` 顶部工具栏（topbar-actions）新增⚙️图标按钮
3. 点击图标 → `setShowSettings(true)` → 渲染 `SettingsModal`
4. `SettingsModal` 的关闭按钮 → `setShowSettings(false)`

**验证**：
- 顶部右侧有⚙️图标
- 点击 → 弹出设置面板
- 设置面板内容完整（接口/模型/确认模式）
- 关闭按钮可关闭

---

## T3: StageView 通用书页容器 + 翻页

**文件**：`app/components/stage-view.tsx`（新建）、`app/page.tsx`、`app/globals.css`
**依赖**：T1

**步骤**：
1. 新建 `stage-view.tsx`：
   - 接受 `stageIndex`、`stageName`、`onPrev`、`onNext`、`children`
   - 顶部：`← 上一页    [stageName]    下一页 →`
   - 内容区域：`children`（书页背景）
   - 第一页无"上一页"，最后一页无"下一页"
2. `page.tsx` 新增翻页逻辑：
   - `STAGE_VIEWS` 数组
   - `navigateStage(direction)` 函数
   - 键盘左右箭头事件监听
3. `globals.css` 新增书页风格样式：
   - `.stage-view` 背景图 `url('/book-page-bg.png')`
   - 古奥字体 `"Noto Serif SC", "SimSun", Georgia, serif`
   - 文字颜色 `#2c1810`
4. 确保书页素材放在 `public/book-page-bg.png`（用户提供）

**验证**：
- StageView 渲染书页背景
- 翻页按钮可切换阶段
- 键盘左右箭头可翻页
- 字体为衬线体

---

## T4: 6 个阶段视图内容实现

**文件**：`app/page.tsx`
**依赖**：T3

**步骤**：
1. 删除旧 `OverviewView` 组件（第 837-979 行）
2. 新增 6 个阶段视图渲染（`view === "stage-background"` 等）：
   - 每个视图用 `StageView` 容器包裹
   - 内容从 `project.analysis` 取数据
   - stage-background：overview 数据（oneLine/cause/history/...）
   - stage-timeplace：timePlace 数据
   - stage-characters：people 数据（卡片网格）
   - stage-characterArcs：characterArcs 数据（每人 article）
   - stage-openingHook：openingHook 文本
   - stage-clues：clues 数据（卡片网格）
3. 翻页按钮接线：`onPrev`/`onNext` 调用 `navigateStage`

**验证**：
- 6 个阶段页面各自显示对应内容
- 内容在书页背景上可读
- 翻页切换正常

---

## T5: 章节总结 UI 删除

**文件**：`app/page.tsx`、删除 `app/components/chapter-summary-view.tsx`
**依赖**：T4

**步骤**：
1. 删除 `app/components/chapter-summary-view.tsx` 文件
2. `page.tsx` 删除：
   - `ChapterSummaryView` 的 import
   - `StructureView` 中"查看总结"按钮（第 510-522 行）和展开逻辑（第 524-531 行）
   - `expandedSummaryId` 状态（第 418-419 行）
   - `buildChapterSummaries` 函数（第 129-216 行）
   - acts 阶段后调用 `buildChapterSummaries` 的代码（第 2150-2156 行）
3. 保留 `ChapterSummary` 类型（`lib/types.ts`）
4. 保留 `ProjectAnalysis.chapterSummaries` 字段（默认空数组）

**验证**：
- 文档结构页无"查看总结"按钮
- `chapter-summary-view.tsx` 已删除
- `npm run build` 通过

---

## T6: prompt 字数要求

**文件**：`app/api/model/route.ts`
**依赖**：无（独立任务）

**步骤**：
1. `background` 阶段 prompt（第 80-96 行）加字数要求：
   - `cause`: 100字左右
   - `history`: 150字左右
   - `currentState`: 100字左右
   - 总计 300-500 字
2. `characterArcs` 阶段 prompt（第 134-140 行）加字数要求：
   - `experience`: 150-200 字
   - `motivation`: 100 字左右

**验证**：
- 代码审查：prompt 中有字数要求
- 跑分析 → 故事背景内容 300-500 字

---

## T7: 人物详情侧滑面板

**文件**：`app/components/person-detail-panel.tsx`（新建）、`app/page.tsx`、`app/components/act-detail-view.tsx`
**依赖**：T4

**步骤**：
1. 新建 `person-detail-panel.tsx`：
   - 从右侧滑入的面板
   - 内容：人物基本信息 + 肖像上传 + 出现在以下幕中（幕标题列表可点击跳转）+ 关联线索
   - 关闭按钮
2. `page.tsx` 接线：
   - `selectedActPersonId` 状态已有
   - 点击人物卡 → `setSelectedActPersonId(personId)`
   - 渲染 `PersonDetailPanel`
3. `act-detail-view.tsx` 无需改（已有 `onSelectPerson` 回调）

**验证**：
- 幕详情中点击人物卡 → 侧滑面板弹出
- 面板显示基本信息+出现记录+关联线索
- 点击出现记录中的幕标题 → 跳转到该幕
- 肖像上传正常

---

## T8: 幕详情书页风格

**文件**：`app/components/act-detail-view.tsx`、`app/globals.css`
**依赖**：T7

**步骤**：
1. `act-detail-view.tsx` 外层容器加书页背景样式
2. `globals.css` 中 `.act-detail-view` 加书页背景
3. 确保幕树状图（`act-tree-view.tsx`）不加书页背景

**验证**：
- 幕详情页面有书页背景
- 幕树状图无书页背景（保持现状）

---

## T9: 全量检查与收尾

**文件**：全部
**依赖**：T1~T8 全部完成

**步骤**：
1. `npm run lint`：零错误
2. `npm run build`：通过
3. `npm test`：通过
4. 检查书页素材 `public/book-page-bg.png` 存在
5. 检查左侧菜单栏和顶部工具栏不受书页风格影响
6. 最终提交

**验证**：
- 全部命令通过
- 书页风格只在 6 个阶段+幕详情
- 其他区域保持原样

---

## 执行规则

- **连续执行**：中间不要汇报进度；只有全部完成或被阻塞才停下
- 按 T1→T9 串行
- 每项一个独立 commit
- 只修任务清单列出的任务，不顺手改
- 写/改文件保持正常缩进换行
- 书页素材 `public/book-page-bg.png` 如果不存在，用占位色（`#f5f0e1`）代替，后续替换

## 完成标志

- 9 个任务全部完成并有验证证据
- `npm run lint`、`npm run build`、`npm test` 全过
- 输出任务清单：每项列出改动文件、内容摘要、验证结果
