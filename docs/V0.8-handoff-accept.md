# 守秘人图谱 v0.8 Handoff → 验收 Agent

## 交付结论

v0.8 已按 `docs/V0.8-task.md` 的 T1→T11 完成开发。最终代码提交为 `c7b4c0f4275de782d3411165c9db01619c299e6d`。

开发侧自动验证全部通过；本地浏览器冒烟测试确认应用首页正常渲染且控制台无错误。浏览器的隔离测试配置中没有现成项目，因此涉及真实剧本、模型调用、拖拽实体窗口和 turn.js 连续翻页的端到端场景留给独立验收 agent 使用测试项目复核。

## T1→T11 修复清单

### T1：共享 Markdown AST、转换与安全层

- Commit：`65eba6a`
- 文件：`lib/markdown.ts`、`app/components/markdown-document.tsx`、`tests/markdown.test.mjs`、`package.json`
- 内容：统一阅读与编辑转换；支持 H1–H6、表格、图片、人物指令、格式 token；加入 HTML、字体和 URL 白名单。
- 验证：Markdown 解析与安全测试通过；lint/build 通过。

### T2：统一目标按钮样式

- Commit：`d32edbc`
- 文件：`app/globals.css`、`app/page.tsx`、`app/components/stage-view.tsx`
- 内容：分析流程三个按钮和书页五个按钮统一为黑底、绿边、白字，补齐 hover/focus/disabled 状态。
- 验证：lint/build 通过。

### T3：扩充人物、人物弧线、开篇钩子和幕输出

- Commit：`7f06f89`
- 文件：`lib/types.ts`、`app/api/model/route.ts`、`app/page.tsx`
- 内容：所有人物支持完整 CoC 7版属性、技能和攻击；每项独立标记 source/inference；新增人物资料、动机变化、主线关系、五字段 openingHookDetails 和 personActions；保持七阶段与 API action 不变。
- 验证：旧字段保持可选；客户端完成范围归一化；lint/build 通过。

### T4：v0.8 默认模板构建器

- Commit：`608f694`
- 文件：`lib/entities.ts`、`tests/templates.test.mjs`
- 内容：新增人物、人物动机、开篇钩子和幕 Markdown 构建器；幕标题使用 `act.sequence`，不再自动把第一项当序幕；旧幕行动从 description 兼容提取。
- 验证：标题、实体引用、开篇钩子和幕行动测试通过。

### T5：人物卡与 CoC 属性展示/编辑

- Commit：`42cdd13`
- 文件：`lib/types.ts`、`lib/entities.ts`、`app/components/markdown-document.tsx`、`app/components/entity-window.tsx`、`app/components/stage-view.tsx`、`app/globals.css`
- 内容：人物卡仅保存实体引用；核心、重要、次要人物分级显示；所有人物可在实体窗口查看完整 CoC 数据；KP 可覆盖属性、技能和攻击；卡片、表格和图片加入分页保护。
- 验证：lint/build 通过。

### T6：新书页结构接线

- Commit：`e78a26b`
- 文件：`app/page.tsx`、`app/components/confirm-wizard.tsx`、`app/components/act-detail-view.tsx`、`app/components/act-text-view.tsx`
- 内容：接入 v0.8 模板；“人物经历与动机”统一改为“核心人物与动机”；“幕描述”改为“本幕剧情”；幕人物行动自动生成实体链接。
- 验证：lint/build 通过。

### T7：可视化 Markdown 编辑器增强

- Commit：`d96e292`
- 文件：`app/components/book-editor.tsx`、`app/globals.css`
- 内容：新增 H1–H6、斜体、下划线、删除线、3×3 表格、URL/本地图片和三种字体；本地图片使用 canvas 限制 1600px、约 1MB，只持久化 Data URL。
- 验证：格式安全测试、lint/build 通过；blob URL 被白名单拒绝。

### T8：旧 sectionMarkdown 非破坏升级

- Commit：`7b2ced7`
- 文件：`lib/types.ts`、`app/page.tsx`、`app/components/book-editor.tsx`、`app/globals.css`
- 内容：已有 Markdown 默认保持不变；目标章节提供“应用 v0.8 模板”，确认后只替换当前编辑草稿；取消编辑不保存；可记录模板版本 8。
- 验证：lint/build 通过。

### T9：多窗口浮动实体资料

- Commit：`8c695e9`
- 文件：`app/components/markdown-document.tsx`、`app/components/entity-window.tsx`、`app/page.tsx`、`app/globals.css`
- 内容：实体资料改为 viewport fixed 羊皮纸窗口；支持多开、拖动、置顶、单窗关闭、返回历史；同实体去重；离开阅读、切换项目或进入编辑时关闭窗口。
- 验证：lint/build 通过；未改变 turn.js 实例和 pages 数组。

### T10：相关实体与出现位置

- Commit：`0dd3a54`
- 文件：`lib/entities.ts`、`app/components/entity-window.tsx`、`app/page.tsx`、`app/globals.css`、`tests/templates.test.mjs`
- 内容：新增 `getRelatedEntities(entityRef, project)`；合并 KP 显式关系、线索指向和同幕共现；人物、线索、地点、事件汇总实际幕出场；窗口常驻显示关系和出现位置。
- 验证：关系/出场自动测试通过；lint/build 通过。

### T11：集成复核

- Commit：`c7b4c0f`
- 文件：`app/components/entity-window.tsx`、`app/globals.css`、`lib/entities.ts`、`lib/markdown.ts`、`tests/markdown.test.mjs`
- 内容：收紧绝对 URL 白名单；修正 KP 单项 CoC 覆盖，避免未修改属性被误标为 KP；补齐人物摘要/组织字段和事件出场标签。
- 验证：最终 lint/build/test 及浏览器冒烟测试通过。

## 改动文件总表

- `app/api/model/route.ts`
- `app/components/act-detail-view.tsx`
- `app/components/act-text-view.tsx`
- `app/components/book-editor.tsx`
- `app/components/confirm-wizard.tsx`
- `app/components/entity-window.tsx`
- `app/components/markdown-document.tsx`
- `app/components/stage-view.tsx`
- `app/globals.css`
- `app/page.tsx`
- `lib/entities.ts`
- `lib/markdown.ts`
- `lib/types.ts`
- `package.json`
- `tests/markdown.test.mjs`
- `tests/templates.test.mjs`

## 最终验证证据

### `npm run lint`

- 结果：通过，0 error。

### `npm run build`

- 结果：通过。
- 路由：`/` 与 `/api/model` 均成功构建。

### `npm run test`

- 结果：8/8 通过。
- 覆盖：应用壳渲染、Markdown 丰富块、安全白名单、标题规则、实体引用模板、开篇/幕兼容、关系与出场推导。

### 浏览器冒烟

- URL：`http://localhost:3000/`
- 结果：成功显示“守秘人图谱”；控制台 error 数量为 0。

## 独立验收重点

1. 使用新项目跑完整七阶段，确认每个核心/重要/次要人物都有完整 CoC 属性，并正确区分原文、推断、KP。
2. 使用旧 v0.7.6 项目确认已有 `sectionMarkdown` 未被覆盖；分别测试模板升级的确认和取消。
3. 在统一书页中连续翻页、跳页、跨阶段/幕导航，并反复离开阅读页再返回，确认无 turn.js DOM 异常。
4. 多开实体窗口，验证拖动、置顶、同实体去重、关联历史、单窗关闭和视图切换清理。
5. 编辑并往返保存 H4–H6、下划线、字体、表格和本地图片，确认分页不截断且 Data URL 可刷新恢复。

## 已知问题

- 开发侧浏览器测试配置没有现成项目，因此没有伪造模型连接或改动用户本地项目来完成端到端测试；上述真实项目交互应由独立验收完成。
- 未发现任务范围内的已知构建或自动测试缺陷。
